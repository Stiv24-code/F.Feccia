# Config env per ambiente (contiene IP admin, path chiavi SSH, email)
config/*.env
!config/*.env.example

# State file locale generato da provision.sh (ID delle risorse AWS create)
.aws-state/

# Artefatti temporanei
*.tmp
*.bak
